<%= form_for @dolist, :html => { :class => "form-horizontal dolist" } do |f| %>

  <% if @dolist.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@dolist.errors.count, "error") %> prohibited this dolist from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @dolist.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :date, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :date, :class => 'form-control' %>
    </div>
    <%=f.error_span(:date) %>
  </div>
  <div class="form-group">
    <%= f.label :do, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :do, :class => 'form-control' %>
    </div>
    <%=f.error_span(:do) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                dolists_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
